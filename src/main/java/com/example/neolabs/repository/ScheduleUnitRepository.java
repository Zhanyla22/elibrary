package com.example.neolabs.repository;

import com.example.neolabs.entity.ScheduleUnit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScheduleUnitRepository extends JpaRepository<ScheduleUnit, Long> {
}
